<style>
    table {
        border-collapse: collapse;
        width: 100%;    
    }

    th, td {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    th {
        background-color: #f2f2f2;
    }
</style>

<table>
    <tr>
        <th>ID</th>
        <th>Identifiant</th>
        <th>Mot de passe</th>
    </tr>

    {% for user in users %}

    <tr>
        <td>{{ user.id }}</td>
        <td>{{ user.identifiant }}</td>
        <td>{{ user.password }}</td>
    </tr>
    {% endfor %}
</table>
